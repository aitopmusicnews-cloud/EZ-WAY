name: Retired Promo Pack route fix
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Promo Pack feature is being removed; this workflow is retired."
